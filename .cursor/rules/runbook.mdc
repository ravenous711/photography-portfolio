---
description: Project runbook & backlog — how to work on this photography portfolio and where tasks are tracked
alwaysApply: true
---

# Photography portfolio — start here

- **`IMPROVEMENTS.md` is the runbook + backlog.** Before starting non-trivial work, skim it:
  the "How to use this runbook" section (dev/test/git conventions), the Status board, the open
  tickets, and the Ideas section (e.g. AUTH-1 — logins/private albums).
- When the user says "do ticket X" (e.g. `BUG-1`, `MNT-1`, `ADMIN-1`), follow that ticket's
  **Why / Fix / Files / Done when** as the brief.
- After shipping a ticket: update its **Status** (ticket + Status board) and add a line to the
  runbook **Changelog**.
- **`README.md`** covers the album *content* workflow (R2 uploads, grid generation, adding
  albums, admin panel). Use it for anything about uploading or managing photos.

## Working conventions (from the runbook)
- Local dev: use the `dev-server` skill (includes required env vars for admin APIs).
- Feature branch per ticket; **commit after each logical step** so any step is easy to revert.
- Production auto-deploys from `main` via Vercel — **push to `main` only when explicitly asked.**
- **Never deploy, publish, or upload files/data to an external endpoint without explicit approval.**

## Access tiers (AUTH-1 — shipped Phase 1–2)
Every album in `js/config.js` has an `audience` field:
- `public` — visible to all; curated subset shown if `curated[]` set, full album is the fallback
- `friends` — curated set public; full album unlocked with friends password (`rf-pix-2026`)
- `family` — hidden album; appears in gallery after family password (`rf-family-pw`)
- `client:<name>` — hidden album; only that client's password works

When creating or editing albums, **always set `audience`**. City albums are typically `friends` (curated set public, full album for friends). Sensitive albums (family/kids/clients) use `family` or `client:<name>` + `hidden: true`.

`PASSWORD_TIERS` in `js/config.js` maps SHA-256 hashes → granted audiences. To add a new client password: hash their password and add an entry. The `adminPasswordHash` field has been removed from config — admin auth is now server-side via `/api/admin-login`.
